# 项目迁移说明

本项目**不依赖固定盘符或用户名**，可拷贝到任意目录（如 `D:\Option_Trader`）或其他电脑使用。

## 需要拷贝的内容

```
Option_Trader/
├── Developer/etf-option-assistant/   # 源码（必拷）
├── Product manager/                  # PRD（可选）
├── UI designer/                      # 原型（可选）
├── 启动ETF期权助手.bat / .ps1
├── 准备ETF期权助手.bat / .ps1
└── scripts/portable.ps1
```

## 不要拷贝（到新电脑后重新生成）

| 目录 | 原因 |
|------|------|
| `backend/.venv/` | 内含旧电脑的绝对路径，无法跨机使用 |
| `frontend/node_modules/` | 体积大，用 `npm install` 重建 |
| `frontend/dist/` | 构建产物，可删 |

`backend/data/`（SQLite、HV 缓存）可拷可不拷：不拷则首次运行自动重建。

## 新电脑 / 新文件夹首次使用

1. 安装 **Python 3.10+**、**Node.js LTS**（加入 PATH）
2. 双击 **`准备ETF期权助手.bat`**（创建 venv、安装依赖）
3. 双击 **`启动ETF期权助手.bat`**

启动脚本会自动检测 `.venv` 是否来自其他路径；若已失效会提示并删除后重建。

## 代码中的路径策略

- Python：`Path(__file__)` 相对项目目录，无硬编码盘符
- 配置：`backend/.env` 使用 `sqlite:///./data/app.db` 相对路径
- 启动脚本：`$PSScriptRoot` 定位项目根目录

## 常见问题

**Q: 拷贝后 Python 报错找不到解释器，或 `No module named 'pip'`？**  
A: 删除 `backend/.venv`，重新运行「准备ETF期权助手」。脚本会用 `python -m pip` 自动修复。

**Q: 前端打不开？**  
A: 在 `frontend` 目录执行 `npm install`，再启动。
